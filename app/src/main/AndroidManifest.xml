<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.artfestproject1">

    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA" /> <!-- in order to access NFC hardware -->
    <uses-permission android:name="android.permission.NFC" /> <!-- show up the application only for devices with NFC hardware -->
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArtFestProject1">
        <activity android:name=".StatusActivity"></activity>
        <activity android:name=".GridViewActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SendMailActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".NfcActivity" />
        <activity android:name=".ShowCamera" />
        <activity android:name=".ExplainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>